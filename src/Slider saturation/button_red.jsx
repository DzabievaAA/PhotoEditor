import React, { useEffect } from 'react';
import WorkSpase from '../WorkSpace/WorkSpace'

function Button_red(getCanvas) {
    useEffect(() => {
        let canvas = getCanvas;
        //let ctx = canvas.getContext('2d');
    })
return <div>
    <button>Red</button>
</div>
}

export default Button_red;